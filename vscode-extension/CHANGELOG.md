# Changelog

## 0.1.0

- Add privacy-safe manual and auto-tracked VS Code AI session capture.
- Record duration, branch, changed-file count, added/removed line counts, and active/idle seconds through the local Halyard CLI.
- Add dashboard launch and current-scope commands.
- Add recovery prompt for unfinished sessions after VS Code restarts.
